#!/bin/sh

export TTL=60
# Expects ETCD to be defined

echo "Updating {{timestamp}}"

{{range services}}
./multiset {{range service .Name}} "{{.Name}}=http://{{.Address}}:{{.Port}}"{{end}}{{end}}


if [ $? -ne 0 ]; then
    echo "There was a problem writing to etcd"
fi
exit 0